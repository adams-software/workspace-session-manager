# router

Very early router v1 scaffold.

Startup shape:

- `router <control-socket-path>`

The control socket path is a required startup parameter, mirroring the explicit local endpoint style used by `msr`.
